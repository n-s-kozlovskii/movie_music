<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.surfcourse.nek.moviemusic.MainActivity">

    <Button
        android:id="@+id/recent_btn"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="15dp"
        android:layout_margin="5dp"
        android:padding="30dp"
        android:text="@string/main_recent_btn"
        android:textAlignment="textStart"
        android:textSize="20sp"/>
    <Button
        android:id="@+id/new_btn"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_margin="5dp"
        android:padding="30dp"
        android:text="@string/main_new_btn"
        android:textAlignment="textStart"
        android:textSize="20sp"/>
    <Button
        android:id="@+id/best_btn"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginBottom="15dp"
        android:layout_margin="5dp"
        android:padding="30dp"
        android:text="@string/main_best_btn"
        android:textAlignment="textStart"
        android:textSize="20sp"/>
</LinearLayout>
